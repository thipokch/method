import 'package:flutter_test/flutter_test.dart';
import 'package:method/<FTName | snakecase&lowercase>/<FTName | snakecase&lowercase>.dart';

void main() {
  group('<FTName | pascalcase>State', () {
    test('Constructing <FTName | pascalcase>State', () {
      const initial = <FTName | pascalcase>State.initial();
      const created = <FTName | pascalcase>State.created();
      const started = <FTName | pascalcase>State.started();
      const resumed = <FTName | pascalcase>State.resumed();
      const destroyed = <FTName | pascalcase>State.destroyed();
    });
  });
}
