name: method_config_service
description: Provides remote, dynamic and personalized, remote configurations for Method app.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:

  flutter:
    sdk: flutter

  # Firebase

  # RudderStack x Firebase SDK version podfile override
  # https://firebase.flutter.dev/docs/manual-installation#overriding-firebase-sdk-versions
  # $FirebaseSDKVersion = '8.15.0'

  firebase_remote_config: ^3.0.8

dev_dependencies:

  # Code Analysis

  method_analysis: 
  
  # Testing

  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  bloc_test: ^9.1.0 
  mocktail: ^0.3.0 