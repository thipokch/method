import 'package:method_style/method_style.dart';

abstract class MtSpaces {
  static const None = MtSizes.size00 + .0;
  static const XS = MtSizes.size01 + .0;
  static const S = MtSizes.size03 + .0;
  static const M = MtSizes.size05 + .0;
  static const L = MtSizes.size07 + .0;
  static const XL = MtSizes.size09 + .0;
  static const Full = MtSizes.size11 + .0;
}
