import 'package:flutter_test/flutter_test.dart';
import 'package:method/entry_edit_converge/entry_edit_converge.dart';

void main() {
  group('EntryEditConvergeState', () {
    test('Constructing EntryEditConvergeState', () {
      // ignore: unused_local_variable
      const initial = EntryEditConvergeState.initial();
      // const created = EntryEditConvergeState.created();
      // const started = EntryEditConvergeState.started();
      // const resumed = EntryEditConvergeState.resumed();
      // const destroyed = EntryEditConvergeState.destroyed();
    });
  });
}
