part of 'session_edit_view.dart';

class SessionEditSliver extends StatelessWidget {
  const SessionEditSliver({super.key});

  @override
  Widget build(BuildContext context) =>
      const SliverToBoxAdapter(child: _Swiper());
}
