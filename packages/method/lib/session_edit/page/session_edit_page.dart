import 'package:flutter/material.dart';

import '../session_edit.dart';

class SessionEditPage extends StatelessWidget {
  const SessionEditPage({super.key});

  @override
  Widget build(BuildContext context) => const SessionEditSliver();
}
