// TODO: Implement AppRoute and AppFlow

import 'package:flutter_test/flutter_test.dart';

void main() {
  // ignore: no-empty-block
  group(
    'AppRoute',
    // ignore: no-empty-block
    () {
      // testWidgets('renders AppRoute', (tester) async {
      //   await tester.pumpWidget(
      //     Builder(
      //       builder: (context) =>
      //           MaterialApp(home: const AppRoute().build(context)),
      //     ),
      //   );
      //   expect(find.byType(AppPage), findsOneWidget);
      // });
    },
    skip: true,
  );
}
