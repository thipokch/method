import 'package:flutter/material.dart';
import 'package:method_model/model.dart';

/// {@template model_sliver}
/// Sliver of the ModelPage.
///
/// Add what it does
/// {@endtemplate}
class ModelSliver extends StatelessWidget {
  /// {@macro model_sliver}
  const ModelSliver({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('ModelPage'));
  }
}
