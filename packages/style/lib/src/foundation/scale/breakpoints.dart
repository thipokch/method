/// Namespace for Default MethodStyle Breakpoints
abstract class MtBreakpoints {
  /// Min width for a small layout.
  static const double small = 0;

  /// Min width for a medium layout.
  static const double medium = 600;

  /// Min width for a large layout.
  static const double large = 840;
}
